---
- name: Update apt cache
  apt:
    update_cache: yes

- name: Install basic packages
  apt:
    name:
      - python3
      - python3-pip
      - git
      - curl
      - unzip
    state: present